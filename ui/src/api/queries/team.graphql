fragment TeamFragment on Team {
  ID
  TeamNumber
  Name
  TeamToCompetition {
    ID
    Name
  }
}

query ListTeams {
  teams {
    ...TeamFragment
  }
}

mutation BatchCreateTeams($teams: [TeamInput!]!) {
  batchCreateTeams(input: $teams) {
    ...TeamFragment
  }
}
